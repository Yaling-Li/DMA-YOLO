# YOLOv5 🚀 by Ultralytics, GPL-3.0 license

# Parameters
nc: 80  # number of classes
depth_multiple: 1.0  # model depth multiple 控制模型的深度（BottleneckCSP个数）
width_multiple: 1.0  # layer channel multiple 控制Conv通道channel个数（卷积核数量）
# depth_multiple表示BottleneckCSP模块的缩放因子，将所有BottleneckCSP模块的Bottleneck乘上该参数得到最终个数。
# width_multiple表示卷积通道的缩放因子，就是将配置里面的backbone和head部分有关Conv通道的设置，全部乘以该系数。
# 通过这两个参数就可以实现不同复杂度的模型设计。

anchors: 4
  # - [10,13, 16,30, 33,23]  # P3/8
  # - [30,61, 62,45, 59,119]  # P4/16
  # - [116,90, 156,198, 373,326]  # P5/32

# YOLOv5 v6.0 backbone
backbone:
  # [from, number, module, args]
  # from：表示当前模块的输入来自哪一层的输出，和darknet里面一致，-1表示来自上一层的输出；
  # number：表示本模块重复的次数，1表示只有一个，3表示重复3次，9表示重复9次；当模块为 C3 或 BottleneckCSP 时，作为内部的 Bottleneck 数量传入
  # module: 模块名称；
  # args: 表示输入参数，比如卷积核个数 卷积核大小 步长stride等；
  [[-1, 1, Conv, [64, 6, 2, 2]],  # 0-P1/2
   [-1, 1, GnConv, [128, 3, 2]],  # 1-P2/4  输出通道，最后一层filter的步长
   [-1, 3, C3, [128]],  # 2
   [-1, 1, GnConv, [256, 3, 2]],  # 3-P3/8 输出通道，最后一层filter的步长
   [-1, 6, C3, [256]],  # 4
   [-1, 1, GnConv, [512, 3, 2]],  # 5-P4/16  输出通道，最后一层filter的步长
   [-1, 9, C3, [512]],  # 6
   [-1, 1, GnConv, [1024, 3, 2]],  # 7-P5/32 输出通道，最后一层filter的步长
   [-1, 3, C3, [1024]], # 8
   [-1, 1, CA, [1024]],  # 9 加入CA注意力模块
   [-1, 1, SPPF, [1024, 5]],  # 10
  ]

# YOLOv5 v6.0 head
# 作者没有区分neck模块，所以head部分包含了PANet+Detect部分
# 由于本份配置其实没有分neck模块，故spp也写在了backbone部分。
head:
  [[-1, 1, SCConv, [512, 1]],  # 11
   [-1, 1, nn.Upsample, [None, 2, 'nearest']], # 12
   [[-1, 6], 1, Concat, [1]],  # 13 cat backbone P4
   [-1, 3, C3, [512, False]],  # 14

   [-1, 1, SCConv, [256, 1]],  # 15
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 16
   [[-1, 4], 1, Concat, [1]],  # 17 cat backbone P3
   [-1, 3, C3, [256, False]],  # 18 (P3/8-small)

   [-1, 1, SCConv, [128, 1]],  # 19
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 20
   [[-1, 2], 1, Concat, [1]],  # 21 cat backbone P2
   [-1, 2, C3HB, [128, False]],  # 22 (P2/4-xsmall)

   [-1, 1, SCConv, [128, 2]],  # 23
   [[-1, 19, 4], 1, Concat, [1]],  # 24 cat head P3
   [-1, 2, C3HB, [256, False]],  # 25 (P3/8-small)

   [-1, 1, SCConv, [256, 2]],  # 26
   [[-1, 15, 6], 1, Concat, [1]],  # 27 cat head P4
   [-1, 2, C3HB, [512, False]],  # 28 (P4/16-medium)

   [-1, 1, SCConv, [512, 2]],  # 29
   [[-1, 11], 1, Concat, [1]],  # 30 cat head P5
   [-1, 2, C3HB, [1024, False]],  # 31 (P5/32-large)

   [[22, 25, 28, 31], 1, Detect, [nc, anchors]],  # Detect(P3, P4, P5)
  ]
